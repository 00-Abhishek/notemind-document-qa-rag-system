# Flexible deployment dependencies for Python 3.12+.
#
# Do not fully pin transitive dependencies here. That can force pip to build
# packages from source when an older pinned release has no wheel for the
# deployment Python version (for example Python 3.14).
faiss-cpu>=1.12.0
langchain>=0.3.27
langchain-community>=0.3.29
langchain-google-genai>=2.1.10
langchain-huggingface>=0.3.1
langchain-text-splitters>=0.3.11
pandas>=2.0.0
pypdf>=6.0.0
python-dotenv>=1.1.1
sentence-transformers>=5.1.0
streamlit>=1.49.1
tiktoken>=0.11.0
